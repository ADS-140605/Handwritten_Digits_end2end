# Frontend

The frontend is a single responsive HTML page served by the FastAPI backend.

## Features

- Draw a digit with the mouse or touch input.
- Paste an image from the clipboard.
- Upload a digit image from disk.
- Pick a model from the dropdown before predicting.
- Launch temporary retraining jobs and switch to the new model when they finish.

No separate build step is required.
